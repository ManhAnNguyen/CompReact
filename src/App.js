import logo from "./logo.svg";
import "./App.css";
import Select, { components } from "react-select";

function App() {
  const handleChange = (val) => {
    console.log(val);
  };

  return <>date picker</>;
}

export default App;
